// CI config: resolve all path params against the project dir so the same
// params files work no matter where the pipeline is launched from.
params.reference_genome = "${projectDir}/assets/tests/test_data/test_reference.fa"
params.query_genome     = "${projectDir}/assets/tests/test_data/test_query.fa"
params.outdir           = "${projectDir}/assets/tests/work/e2e_results"

// skip pipeline_info reporting (would write to <repo>/results regardless of outdir)
report { enabled = false }
timeline { enabled = false }
trace { enabled = false }
dag { enabled = false }
