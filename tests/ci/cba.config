// CI config: resolve all path params against the project dir so the same
// params files work no matter where the pipeline is launched from.
params.reference_genome = "${projectDir}/tests/fixture/cba/reference.fa"
params.query_genome     = "${projectDir}/tests/fixture/cba/query.fa"
params.filled_chain_path = "${projectDir}/tests/fixture/cba/filled.chain"
params.outdir           = "${projectDir}/tests/work/cba_results"

// skip pipeline_info reporting (would write to <repo>/results regardless of outdir)
report { enabled = false }
timeline { enabled = false }
trace { enabled = false }
dag { enabled = false }
