/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    make_lastz_chains Nextflow configuration
    nf-core style: all pipeline parameters with defaults, profiles for different
    execution environments (local, slurm, conda, apptainer).
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
*/

nextflow.enable.dsl = 2

manifest {
    name            = 'make_lastz_chains'
    author          = 'Bogdan M. Kirilenko, Michael Hiller, Virag Sharma, Ekaterina Osipova'
    homePage        = 'https://github.com/hillerlab/make_lastz_chains'
    description     = 'Pipeline to create chain-formatted pairwise genome alignments.'
    mainScript      = 'main.nf'
    nextflowVersion = '!>=23.04.0'
    version         = '3.0.0'
}

// ── Pipeline parameters ────────────────────────────────────────────────────
params {

    // ── Input / Output ──────────────────────────────────────────────────────
    target_name     = null      // e.g. 'hg38'
    query_name      = null      // e.g. 'mm39'
    target_genome   = null      // path to target genome (FASTA or .2bit)
    query_genome    = null      // path to query genome (FASTA or .2bit)
    outdir          = './results'
    publish_dir_mode = 'copy'

    // ── Genome partitioning ─────────────────────────────────────────────────
    seq1_chunk      = 175000000  // target chunk size (bp)
    seq2_chunk      = 50000000   // query chunk size (bp)
    seq1_lap        = 0          // target chunk overlap (bp)
    seq2_lap        = 10000      // query chunk overlap (bp)
    seq1_limit      = 5000       // target sequence length filter
    seq2_limit      = 10000      // query sequence length filter

    // ── LASTZ alignment ─────────────────────────────────────────────────────
    lastz_y         = 9400       // gap extension penalty
    lastz_h         = 2000       // seed hit count
    lastz_l         = 3000       // step length
    lastz_k         = 2400       // minimum score for anchor alignments
    lastz_q         = null       // optional substitution score matrix file

    // ── Chain building ──────────────────────────────────────────────────────
    min_chain_score  = 1000      // minimum chain score (axtChain -minScore)
    chain_linear_gap = 'loose'   // linearGap model: 'loose' or 'medium'
    chaining_memory  = 50        // GB per axtChain job
    bundle_psl_max_bases = 1000000  // max bases per PSL bundle

    // ── Fill chains (optional step) ─────────────────────────────────────────
    skip_fill_chains        = false
    skip_fill_unmask        = false
    num_fill_jobs           = 1000
    fill_chain_min_score    = 25000
    fill_insert_chain_min_score = 5000
    fill_gap_max_size_t     = 20000
    fill_gap_max_size_q     = 20000
    fill_gap_min_size_t     = 30
    fill_gap_min_size_q     = 30
    fill_lastz_k            = 2000
    fill_lastz_l            = 3000
    fill_memory             = 16   // GB per repeat-filler job

    // ── Clean chains (optional step) ────────────────────────────────────────
    skip_clean_chain        = false
    chain_clean_memory      = 100  // GB for chainCleaner
    clean_chain_parameters  = '-LRfoldThreshold=2.5 -doPairs -LRfoldThresholdPairs=10 -maxPairDistance=10000 -maxSuspectScore=100000 -minBrokenChainScore=75000'

    // ── Execution / cluster ─────────────────────────────────────────────────
    cluster_executor        = 'local'
    cluster_queue           = 'batch'
    job_time_req            = '48h'

    // ── Tool paths (override if not in PATH) ────────────────────────────────
    lastz_path              = 'lastz'
    axt_to_psl_path         = 'axtToPsl'

    // ── nf-core boilerplate ──────────────────────────────────────────────────
    validate_params         = true
    show_hidden_params      = false
    help                    = false
    version                 = false
    monochrome_logs         = false
    hook_url                = null
    email                   = null
    email_on_fail           = null
    plaintext_email         = false
    max_multiqc_email_size  = '25.MB'

    // Max resource limits (safety ceiling)
    max_memory              = '500.GB'
    max_cpus                = 16
    max_time                = '240.h'
}

// ── Include base resource config ───────────────────────────────────────────
includeConfig 'conf/base.config'

// ── Include per-process module config ─────────────────────────────────────
includeConfig 'conf/modules.config'

// ── Profiles ───────────────────────────────────────────────────────────────
profiles {

    // ── Local execution (default) ───────────────────────────────────────────
    local {
        process.executor = 'local'
    }

    // ── SLURM cluster ───────────────────────────────────────────────────────
    slurm {
        process.executor = 'slurm'
        process.queue    = params.cluster_queue
        executor {
            queueSize    = 1000
            submitRateLimit = '10/1min'
        }
    }

    // ── SGE cluster ─────────────────────────────────────────────────────────
    sge {
        process.executor = 'sge'
        process.queue    = params.cluster_queue
    }

    // ── Conda environment ───────────────────────────────────────────────────
    conda {
        conda.enabled       = true
        docker.enabled      = false
        singularity.enabled = false
        apptainer.enabled   = false
        // Per-process conda paths are set in conf/modules.config (withName blocks)
    }

    // ── Apptainer ───────────────────────────────────────────────────────────
    apptainer {
        apptainer.enabled    = true
        apptainer.autoMounts = true
        docker.enabled       = false
        singularity.enabled  = false
        conda.enabled        = false
        // Per-process container paths are set in conf/modules.config (withName blocks)
    }

    singularity {
        singularity.enabled    = true
        singularity.autoMounts = true
        docker.enabled         = false
        apptainer.enabled      = false
        conda.enabled          = false
        // TODO: fill in per-module container URIs when user provides them
    }

    docker {
        docker.enabled      = true
        singularity.enabled = false
        apptainer.enabled   = false
        conda.enabled       = false
        // TODO: fill in per-module Docker image URIs when user provides them
    }

    // ── Test profile (small genomes in test_data/) ──────────────────────────
    test {
        params.target_name   = 'test_ref'
        params.query_name    = 'test_query'
        params.target_genome = "${projectDir}/test_data/test_reference.fa"
        params.query_genome  = "${projectDir}/test_data/test_query.fa"
        params.outdir        = 'test_results'
        // Use smaller chunk sizes so the test finishes quickly
        params.seq1_chunk    = 10000000
        params.seq2_chunk    = 10000000
        params.chaining_memory  = 4
        params.fill_memory      = 4
        params.chain_clean_memory = 4
    }
}

// ── Execution trace and reports ────────────────────────────────────────────
def trace_timestamp = new java.util.Date().format('yyyy-MM-dd_HH-mm-ss')

timeline {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_timeline_${trace_timestamp}.html"
}

report {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_report_${trace_timestamp}.html"
}

trace {
    enabled = true
    file    = "${params.outdir}/pipeline_info/execution_trace_${trace_timestamp}.txt"
}

dag {
    enabled = true
    file    = "${params.outdir}/pipeline_info/pipeline_dag_${trace_timestamp}.html"
}

// ── Default process settings ───────────────────────────────────────────────
process {
    // Override executor from cluster_executor param if set
    executor = params.cluster_executor != 'local' ? params.cluster_executor : 'local'

    // Shell settings
    shell = ['/bin/bash', '-euo', 'pipefail']
}

// ── Global environment ─────────────────────────────────────────────────────
env {
    PYTHONNOUSERSITE = 1
    R_PROFILE_USER   = "/.Rprofile"
    R_ENVIRON_USER   = "/.Renviron"
    JULIA_DEPOT_PATH = "/usr/local/share/julia"
}
